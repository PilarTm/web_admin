// Generated by CoffeeScript 1.4.0

$(document).ready(function() {
  $('.left-nav-toggle a').on('click', function(event) {
    event.preventDefault();
    return $("body").toggleClass("nav-toggle");
  });
  $('.nav-second').on('show.bs.collapse', function() {
    return $('.nav-second.in').collapse('hide');
  });
  $('.panel-toggle').on('click', function(event) {
    var body, footer, hpanel, icon;
    event.preventDefault();
    hpanel = $(event.target).closest('div.panel');
    icon = $(event.target).closest('i');
    body = hpanel.find('div.panel-body');
    footer = hpanel.find('div.panel-footer');
    body.slideToggle(300);
    footer.slideToggle(200);
    icon.toggleClass('fa-chevron-up'.toggleClass('fa-chevron-down'));
    hpanel.toggleClass(''.toggleClass('panel-collapse'));
    return setTimeout(function() {
      hpanel.resize();
      return hpanel.find('[id^=map-]'.resize());
    }, 50);
  });
  return $('.panel-close').on('click', function(event) {
    var hpanel;
    event.preventDefault();
    hpanel = $(event.target).closest('div.panel');
    return hpanel.remove();
  });
});
